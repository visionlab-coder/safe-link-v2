# ── Pusher (https://pusher.com 에서 무료 앱 생성)
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=ap3

# ── Pusher 클라이언트용 (NEXT_PUBLIC_ 필수)
NEXT_PUBLIC_PUSHER_KEY=
NEXT_PUBLIC_PUSHER_CLUSTER=ap3

# ── Google Cloud Translation API
# (기존 SAFE-LINK에서 사용 중인 키 그대로 사용)
GOOGLE_TRANSLATE_API_KEY=
