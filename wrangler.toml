#:schema node_modules/wrangler/config-schema.json
name = "safe-link-v2"
main = ".open-next/worker.js"
compatibility_date = "2024-12-30"
compatibility_flags = ["nodejs_compat"]

# NEXT_PUBLIC_ 및 외부 공개 가능한 식별자(client_id, public key, cluster 등)는
# 여기 박아둬야 Workers 런타임의 process.env 접근이 100% 안정적임.
# (Cloudflare Dashboard 의 SECRET 으로만 등록하면 런타임에서 간헐적으로 누락되는 케이스 관찰됨)
# 실제 비밀 키(SERVICE_ROLE_KEY, NAVER_CLIENT_SECRET, PUSHER_SECRET 등)는 Dashboard 에서 secret 으로 유지.
[vars]
NEXT_PUBLIC_SUPABASE_URL = "https://psczxaaqxkzzvnrgawhl.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY = "sb_publishable_bkyJFFs4Ij3QsccXa9fr5A_qColrn8D"
NAVER_CLIENT_ID = "pff1l7x9oe"
PUSHER_KEY = "4966c6c20c77ffa87d75"
PUSHER_CLUSTER = "ap3"

[assets]
directory = ".open-next/assets"
binding = "ASSETS"
